@model VirtualVendingMachine.Data.DTO.CoinChangesDto
@for (var i = 0; i < Model.CoinChanges.Count; i++)
{
    <div class="row form-group">
        <label class="col-md-12"><i class="fa fa-eur">@Model.CoinChanges[i].CurrencyName</i> total pieces : @Model.CoinChanges[i].Pieces</label>
    </div>
}
